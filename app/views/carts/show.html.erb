<h2>Cart</h2>
<ul>
	<% @cart.line_items.each do |item| %>
		<li><%= item.food.name %> x <%= item.quantity %> = <%= item.total_price %></li>
		<li>Итого: <%= item.total_price %> сом</li>
	<% end %>
</ul>

<%= button_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' } %>
